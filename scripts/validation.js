document.addEventListener('DOMContentLoaded',()=>{const form=document.querySelector('#contactForm');if(form){form.addEventListener('submit',e=>{let errors=[];const name=form.sender_name.value.trim();const email=form.sender_email.value.trim();const message=form.message.value.trim();if(name.length<2)errors.push('Name must contain at least 2 characters.');if(!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email))errors.push('Please enter a valid email address.');if(message.length<10)errors.push('Message must contain at least 10 characters.');const box=document.querySelector('#clientErrors');box.innerHTML='';if(errors.length){e.preventDefault();box.className='notice error';box.innerHTML=errors.join('<br>');}})}});